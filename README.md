# dynamic-table-editor-vaadin
A tool that connects to an embedded H2 database and reads metadata from INFORMATION_SCHEMA.TABLES and INFORMATION_SCHEMA.COLUMNS to show a list of tables.  Then, it allows the user to click a table which auto-generates a form and data grid for editing/inserting rows.
